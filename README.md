### A bootstrap script for gavento/dotfiles repo to be used with devpod (and possibly other containers).

Installs zsh, git, and curl, sets shell to zsh, and then prodeeds to install dotfiles via the `dotfiles bootstrap-apt` script.
